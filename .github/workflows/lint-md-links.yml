---
name: Lint MD and Links

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  # issues: write is required at startup by the reusable's notify job, even
  # though notify_on_failure defaults to false and the job never runs here.
  # GitHub validates all reusable job permissions before evaluating if-conditions.
  issues: write

jobs:
  lint:
    uses: qte77/.github/.github/workflows/lint-md-links.yml@9ed630f21c0f3ae0285bf621e73a8dc5d086b4bf  # 2026-05-17
...
